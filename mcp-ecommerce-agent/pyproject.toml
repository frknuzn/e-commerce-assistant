[project]
name = "mcp-ecommerce-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.9.3",
    "python-dotenv>=1.1.0",
]

[tool.uv.workspace]
members = [
    "mcp-client",
]
